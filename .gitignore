# Runtime data directory (generated on first login)
data/

# Password hash generated from config admin_password
data/admin.hash

# User accounts file (contains passwords - do not commit)
users.php

# Server-specific config (copy config.example.php and rename to config.php)
config.php

# PHP built-in server artifacts
*.log
